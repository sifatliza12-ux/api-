/**
 * Single source of truth for the demo payment details shown to buyers
 * during the manual-approval purchase flow (marketplace purchase dialog,
 * My Purchases resubmit dialog). Update the number here rather than in
 * each page that displays payment instructions.
 */
self.FORGEFLOW_PAYMENT_CONFIG = {
    bkash: { number: '01319676750' }
};
